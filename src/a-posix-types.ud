(section
  (ref "posix_types")
  (title "Base definitions")

  (para "The definitions given here are intended to match the basic
    Ada types. They are essentially just included to allow the formal
    Z specification to pass type checks.")

  (para-verbatim z_specification (render-noescape "Z/posix-ada-types")))
